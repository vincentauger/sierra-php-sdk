# Sierra API Configuration
# These are required for testing against a real Sierra API instance
# By default, tests use fixtures, so this is optional

SIERRA_CLIENT_KEY=your_client_key_here
SIERRA_CLIENT_SECRET=your_client_secret_here
SIERRA_API_URL=https://your.sierra.server/iii/sierra-api/v6

# Set to true to enable real API testing (requires valid credentials above)
ENABLE_IN_TESTS=false